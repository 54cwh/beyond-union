# 协作说明

5 人协作开发北域绿联（新能源经营决策工作台）。靠"宪法级规范 + 契约测试"保证步调一致、互不打架。

---

## 开工前必读（顺序阅读）

1. **`需求.md`** —— 产品需求（"经营决策工作台"），先理解产品再动手
2. **`design-system/MASTER.md`** —— 视觉宪法（配色/字体/风格），写页面严格遵循；**由 B（展示岗）维护**，其他人只读
3. **`template.html`** —— 标准页面骨架，**从它复制**开始做你的页面，不要自己重新搭
4. **`技术栈定案.md`** —— 锁死细则、每页启动流程、版本锁定表
5. **`分工说明.md` → "岗位工作区"** —— **先看自己的工作区**（分支/负责文件/文档位置），再往下读依赖、关键路径、里程碑
6. 本说明的"文件所有权表" —— 知道你能动什么、只能读什么

---

## 环境准备（一次）

```bash
# 拉取仓库（Gitee/GitHub 托管位置确认后）
git clone <仓库地址>
cd <项目目录>

# Python（uv 管理，统一 3.12；Windows 成员装 uv 后命令一致）
uv sync                                   # 安装全部依赖
uv run playwright install chromium        # 测试浏览器内核

# Node（Vitest 单测；Windows/WSL 成员装 Node 18+ 后命令一致）
npm install

# Tailwind 编译（本机需下载 v3.4 standalone CLI，见 scripts/build_tailwind.sh）
```

## 提交前必跑（缺一不可）

```bash
npm test               # JS 纯函数单测（Vitest）
uv run pytest          # E2E + 契约测试（Playwright）
uv run ruff check .    # Python 代码规范
```

---

## 打包（角色 E，Windows 或 Wine 环境执行）

```bash
bash scripts/build_exe.sh                 # 产物 → dist/北域绿联.exe
# 产物含全部资源（页面/JSON/字体/vendor），离线可运行
# 验收：Windows 成员双击 EXE 验 GUI；Wine 下 dist/北域绿联.exe --web 验浏览器模式
```

> PyInstaller 不跨平台：Windows EXE 须在 Windows 或 Wine+Windows Python 下打包。

---

## 提前声明（开工前须知，避免返工）

0. **项目背景**：这是"新能源企业经营决策工作台" demo（详见 `需求.md`），页面结构为 **p1-p6 共 6 页**（见 `分工说明.md` 页面结构表），强调"决策链路 + 可信 AI"，不是普通数据展示。
0. **禁止绝对路径（迁移性）**：代码、文档、脚本中**一律禁止出现本机绝对路径**（如 `/home/xxx/`、`C:\Users\xxx\`、`/mnt/c/`）。本项目会被 clone 到任意机器运行，写死本机路径会导致他人环境直接失效。**路径约定**：页面内部引用共享资源用相对路径（`../assets/`）；导航链接与数据加载（common.js 内）用**站点根路径**（`/pages/...`、`/data/...`），因为页面在 `/pages/` 子目录，相对路径会解析成 `/pages/pages/` 导致 404。
1. **图表统一走展示组件**：图表页不直接 `echarts.init`，统一通过 B 维护的 `chart-view.js` 渲染（模板已示范用法，照抄即可）。聚合→图表的衔接用 A 提供的计算层 API。**具体接口设计由 A/B 决定**，模板只是参考实现。
2. **数据用 A 的模拟数据集**：A（数据岗）会先产出**演示模拟数据**（`data/demo/*.json`，含天气/发电/储能/市场/收益全链路），页面基于它开发，不依赖真实数据。真实数据到位后 A 用 pandas ETL 替换，页面只改数据源。
3. **AI 结论是可模拟的**：AI 副驾驶与"可信 AI 八要素"由 E 的问答库 + 预置数据实现，**不是真 AI**。页面展示 AI 建议卡片时用 B 的可信 AI 卡片组件（结论/依据/置信度等），不自己编逻辑。
4. **Git 仓库已本地初始化**（main 分支已提交骨架）；**远程托管位置待定**（Gitee/GitHub），定好后推送一次，成员 clone 开工。
5. **提交时机**：每人 push 到自己的分支（`feature/a`…`feature/e`），**不要直接动 main**；是否合并由 E 决定。
6. **共用文件三原则**：领域内文件直接改（领域自治）；只读文件（vendor/MASTER 的他人视角）别改；**改接口须同步文档并通知消费方**（见所有权表）。

## 常用命令

```bash
# 开发预览（浏览器直开页面）
uv run python -m http.server 8000
# 打开 http://127.0.0.1:8000/pages/p1.html

# JS 纯函数单测
npm test

# E2E + 契约测试
uv run pytest

# 编译 Tailwind（改过页面 class 后必须跑）
bash scripts/build_tailwind.sh
```

---

## 技术栈

**详见 `技术栈定案.md`（唯一权威），这里只列执行要点：**

| 层 | 约定 |
|----|------|
| 样式 | Tailwind v3.4（预编译 `assets/tailwind.css`）+ `common.css` |
| JS | Vue 3.4 全局版 · **Options API**（禁 setup() / 禁 import Vue / 禁 SFC）|
| 图表 | ECharts 5.5（本地 vendor 文件）|
| 数据 | `data/demo/*.json` 模拟数据 + fetch（A 产出）|
| 打包 | pywebview 双模式（GUI 默认 + `--web` 兜底）|

**每页启动流程、版本锁定、测试体系、依赖清单 → 见 `技术栈定案.md`，模板已锁死，照抄 `template.html` 即可。**

---

## 文件所有权表（谁能动什么）

**核心模型：领域自治 + 接口协调。**
- **领域自治**：每个岗位对其**领域内文件有完全决定权**（可自由改、不需审批）。前端风格归 B、数据归 A、页面归 owner、基础设施归 E。
- **接口协调**：仅当改动**影响他人消费的接口**（数据契约字段、chart-view 入参、common.js 导出、视觉风格类）时，须与消费方对齐；跨领域冲突由 E 协调裁定。
- **页面私有文件** = owner 全权，可自由拆小文件；**公共只读**（vendor/锁定文件）= 审批前不动。

| 文件 | 领域负责人（完全决定权）| 他人 | 备注 |
|------|-----------|------|------|
| `pages/p1.html` | B | 只读 | 运营工作台（01+02+03 能源地图）|
| `pages/p2.html` | D | 只读 | 发电预测中心（04）|
| `pages/p3.html` | D | 只读 | 风光储优化中心（05）|
| `pages/p4.html` | C | 只读 | 绿电入市决策中心（06+07，核心）|
| `pages/p5.html` | E | 只读 | 决策与政策中心（08+09）|
| `pages/p6.html` | **A（数据岗）** | 只读 | 经营成效中心（10+11+12）|
| `data/*.json` + 数据契约 | **A（数据岗）** | 只读 | 页面按契约字段取数 |
| `data/demo/*.json`（模拟数据）| **A（数据岗）** | 只读 | 演示数据集 |
| `data/CONTRACT.md`（数据契约文档）| **A（数据岗）** | 只读 | 给 B/C/D/E 的取数依据 |
| `assets/js/filter.js`（数据计算）| **A（数据岗）** | 只读 | 计算层；**改接口须通知消费方** |
| `assets/js/format.js`（格式化）| **A（数据岗）** | 只读 | 计算层；**改接口须通知消费方** |
| `assets/js/chart-view.js`（统一展示）| **B（展示岗）** | 只读 | 图表组件；**改接口须通知消费方** |
| `assets/js/ai-card.js`（可信 AI 卡片）| **B（展示岗）** | 只读 | ⏳ 创建，跨页组件 |
| `assets/common.css`（前端风格）| **B（展示岗）** | 只读 | **前端视觉风格由 B 决定** |
| `tailwind.config.js`（样式配置）| **B（展示岗）** | 只读 | 视觉风格的一部分 |
| `assets/tailwind.css` | B | 只读 | **合并前最终编译由 B 执行** |
| `assets/tailwind.src.css`（Tailwind 输入源）| **B（展示岗）** | 只读 | 随样式走 |
| `scripts/build_tailwind.sh`（编译脚本）| **B（展示岗）** | 只读 | 编译随样式归 B |
| `design-system/MASTER.md`（视觉宪法）| **B（展示岗）** | 只读 | 前端风格唯一权威，B 维护 |
| `assets/js/ai-qa.js`（AI 问答库）| **E（集成岗）** | 只读 | ⏳ 创建，预置文案库 |
| `assets/js/copilot.js`（全局组件壳）| E | 只读 | ⏳ 创建，右侧固定 |
| `assets/js/common.js`（基础设施）| E | 只读 | **改导出/接口须通知消费方** |
| `template.html` | E | 只读 | 标准骨架 |
| `scripts/build_exe.sh`（打包脚本）| E | 只读 | 打包随集成归 E |
| `tests/conftest.py`（测试基建）| E | 只读 | 测试服务器 fixture |
| `tests/test_contract.py`（契约测试）| E | 只读 | 跨模块一致性 |
| `北域绿联.spec`（PyInstaller 配置）| E | 只读 | 跨平台打包配置 |
| `.gitattributes` / `.gitignore`（git 配置）| E | 只读 | 仓库基建 |
| `pyproject.toml` / `package.json`（依赖）| E | 只读 | 新增依赖仍走 E 审批 |
| `README.md` | E | 只读 | 项目介绍 |
| `协作说明.md` / `分工说明.md` | E | 只读 | 协作流程文档 |
| `技术栈定案.md` | E | 只读 | 技术决策记录（变更团队知晓）|
| `需求.md` | 团队 | 提议 | 产品需求，非技术文件，人人可提变更 |
| `assets/vendor/*` | 全员 | **只读** | 锁定版本，审批前不动 |

> **接口变更纪律**：领域内文件可直接改，但**若改变了接口**（函数签名/字段名/导出/数据形态），必须：① 同步更新对应接口文档；② 在群里通知消费方；③ 消费方页面跑测试确认无波及。
> **跨领域冲突**（如 A 的数据形态与 B 的图表组件冲突）→ 由 E 协调裁定，不擅自互改对方文件。

> **页面归属已定**（见 `分工说明.md` 页面结构表）：B=p1，D=p2+p3，C=p4，E=p5，A=p6。
> **每人必写文档**：见 `分工说明.md`"岗位文档职责"章节（接口文档随代码同分支提交）。
> **文档所有权**：各岗位必写的说明文档（如 `assets/js/chart-view.md`、`pages/p1.md`、`scripts/打包说明.md` 等）**与对应代码文件同权**——页面文档归页面 owner，共享模块文档归该模块维护者，无需单独登记，随代码同分支提交。

**模块化原则**：页面 owner 在自己页面内自由拆分小文件；新增全局共享模块 → 归对应领域负责人，跨领域新增由 E 协调。

---

## 5 人分工

**岗位职责、数据流分工、关键路径与里程碑 → 见 `分工说明.md`（唯一权威）。**

> 本文件只讲执行规则（所有权、Git、测试、验收）；谁干什么见分工说明。

---

## Git 流程

- 每人一个分支：`feature/a` → `feature/e`
- **E 统一合并到 main**
- **合并冻结点：Day2 上午 11:00**（此后各分支冻结，不再提交功能改动，只允许排障修复；见 `分工说明.md` 里程碑）
- 合并门槛（缺一不可）：
  1. 自动化测试全绿（`npm test` + `uv run pytest`）
  2. **人工浏览器实操检查**：`uv run python -m http.server 8000` 打开页面亲手点一遍核心流程
  3. E 审批合并

---

## 测试体系（三层）

**详见 `技术栈定案.md`，执行要点：**

| 层 | 工具 | 文件 |
|----|------|------|
| 函数单测 | Vitest | `assets/js/__tests__/*.test.js` |
| 模块 E2E + 共享契约 | pytest + Playwright | `tests/test_pX.py` + `tests/test_contract.py` |
| 打包回归 | 起 EXE + 探测 | 打包后验证 |

---

## 验收分工

| 验收 | 负责人 |
|------|--------|
| `--web` 浏览器模式全功能 | Linux/Wine 负责人 |
| GUI 窗口（WebView2）真机 | Windows 成员 |
| 打包 EXE | Linux/Wine（Wine 跑 Windows Python + PyInstaller）|

> PyInstaller 不跨平台：Linux 打不出 Windows EXE，须 Wine 或 Windows 环境。

---

## 待确认（开工前/数据到手后）

- **真实数据**（如有）→ A 用 pandas ETL 替换模拟数据；无真实数据则用模拟数据集演示
- **各页交互细节** → 由页面负责人按需求.md 自行设计（p1-p6）
- **Git 远程托管位置**（Gitee/GitHub，本地已 init）

---

## 2 天时间线

**里程碑排期（按岗位分列）→ 见 `分工说明.md`。**
