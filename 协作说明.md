# 协作说明

5 人协作开发挑战杯 Demo。靠"宪法级规范 + 契约测试"保证步调一致、互不打架。

---

## 开工前必读（顺序阅读）

1. **`design-system/MASTER.md`** —— 视觉宪法（配色/字体/风格），写页面严格遵循，只读勿改
2. **`template.html`** —— 标准页面骨架，**从它复制**开始做你的页面，不要自己重新搭
3. **`技术栈定案.md`** —— 锁死细则、每页启动流程、版本锁定表
4. **`分工说明.md`** —— 你的岗位、依赖关系、关键路径、里程碑
5. 本说明的"文件所有权表" —— 知道你能动什么、只能读什么

---

## 环境准备（一次）

```bash
# 拉取仓库（Gitee/GitHub 托管位置确认后）
git clone <仓库地址>
cd <项目目录>

# Python（uv 管理，统一 3.12；Windows 成员装 uv 后命令一致）
uv sync                                   # 安装全部依赖
uv run playwright install chromium        # 测试浏览器内核

# Node（Vitest 单测；Windows/WSL 成员装 Node 18+ 后命令一致）
npm install

# Tailwind 编译（本机需下载 v3.4 standalone CLI，见 scripts/build_tailwind.sh）
```

## 提交前必跑（缺一不可）

```bash
npm test               # JS 纯函数单测（Vitest）
uv run pytest          # E2E + 契约测试（Playwright）
uv run ruff check .    # Python 代码规范
```

---

## 打包（角色 E，Windows 或 Wine 环境执行）

```bash
bash scripts/build_exe.sh                 # 产物 → dist/挑战杯Demo.exe
# 产物含全部资源（页面/JSON/字体/vendor），离线可运行
# 验收：Windows 成员双击 EXE 验 GUI；Wine 下 dist/挑战杯Demo.exe --web 验浏览器模式
```

> PyInstaller 不跨平台：Windows EXE 须在 Windows 或 Wine+Windows Python 下打包。

---

## 提前声明（开工前须知，避免返工）

1. **图表统一走展示组件**：图表页不直接 `echarts.init`，统一通过 B 维护的 `chart-view.js` 渲染（模板已示范用法，照抄即可）。聚合→图表的衔接用 A 提供的计算层 API。**具体接口设计由 A/B 决定**，模板只是参考实现。
2. **数据契约分两阶段**：A 先出"契约骨架"（字段名/类型/可筛字段），再补真实数据。
   骨架未出前 `data/sample.json` 只是占位，**页面开发不要依赖它的字段名**；各页先按通用类型开发 UI。
3. **Git 仓库已本地初始化**（main 分支已提交骨架）；**远程托管位置待定**（Gitee/GitHub），定好后推送一次，成员 clone 开工。
4. **提交时机**：每人 push 到自己的分支（`feature/a`…`feature/e`），**不要直接动 main**；是否合并由 E 决定。
5. **共用文件三原则**：只读的别改；要改先提给维护者；新增共享模块走 E 审批（见所有权表）。

## 常用命令

```bash
# 开发预览（浏览器直开页面）
uv run python -m http.server 8000
# 打开 http://127.0.0.1:8000/pages/p1.html

# JS 纯函数单测
npm test

# E2E + 契约测试
uv run pytest

# 编译 Tailwind（改过页面 class 后必须跑）
bash scripts/build_tailwind.sh
```

---

## 技术栈

**详见 `技术栈定案.md`（唯一权威），这里只列执行要点：**

| 层 | 约定 |
|----|------|
| 样式 | Tailwind v3.4（预编译 `assets/tailwind.css`）+ `common.css` |
| JS | Vue 3.4 全局版 · **Options API**（禁 setup() / 禁 import Vue / 禁 SFC）|
| 图表 | ECharts 5.5（本地 vendor 文件）|
| 数据 | `data/*.json` + fetch（结构待定）|
| 打包 | pywebview 双模式（GUI 默认 + `--web` 兜底）|

**每页启动流程、版本锁定、测试体系、依赖清单 → 见 `技术栈定案.md`，模板已锁死，照抄 `template.html` 即可。**

---

## 文件所有权表（谁能动什么）

**页面私有文件 = owner 全权，可自由拆小文件；共享基础设施 = 默认维护者 + 变更走 E 审批；公共只读 = 审批前不动。**
**岗位边界与决策权：见 `分工说明.md` 的"岗位边界"章节（计算 A 定、图表 B 定、页面 C/D 定、公共 E 定）。**

| 文件 | 默认维护者 | 他人 | 备注 |
|------|-----------|------|------|
| `pages/p1.html` | A | 只读 | 首页 |
| `pages/p2.html` | B | 只读 | 看板 |
| `pages/p3.html` | C | 只读 | 组合查询 |
| `pages/p4.html` | D | 只读 | 明细搜索 |
| `pages/p5.html` | E | 只读 | 结论 |
| `data/*.json` + 数据契约 | **A（数据岗）** | 只读 | 页面按契约字段取数 |
| `assets/js/filter.js`（数据计算）| **A（数据岗）** | 提议改 → E 审批 | 计算层 |
| `assets/js/format.js`（格式化）| **A（数据岗）** | 提议改 → E 审批 | 计算层 |
| `assets/js/chart-view.js`（统一展示）| **B（展示岗）** | 提议改 → E 审批 | 只渲染不计算 |
| `assets/js/query-builder.js` | C | 提议改 → E 审批 | 只做绑定；计算调 A 的（⏳ 数据契约定后创建）|
| `assets/js/common.js` | E | 提议改 → E 审批 | 基础设施 |
| `template.html` | E | 只读 | 标准骨架 |
| `assets/vendor/*` | 全员 | **只读** | 锁定版本 |
| `assets/tailwind.css` | E | 只读 | 编译归 E |
| `design-system/MASTER.md` | 全员 | **只读** | 视觉宪法 |

**模块化原则**：页面 owner 在自己页面内自由拆分小文件；新增全局共享模块 → 向 E 提，审批后合入。

---

## 5 人分工

**岗位职责、数据流分工、关键路径与里程碑 → 见 `分工说明.md`（唯一权威）。**

> 本文件只讲执行规则（所有权、Git、测试、验收）；谁干什么见分工说明。

---

## Git 流程

- 每人一个分支：`feature/a` → `feature/e`
- **E 统一合并到 main**
- 合并门槛（缺一不可）：
  1. 自动化测试全绿（`npm test` + `uv run pytest`）
  2. **人工浏览器实操检查**：`uv run python -m http.server 8000` 打开页面亲手点一遍核心流程
  3. E 审批合并

---

## 测试体系（三层）

**详见 `技术栈定案.md`，执行要点：**

| 层 | 工具 | 文件 |
|----|------|------|
| 函数单测 | Vitest | `assets/js/__tests__/*.test.js` |
| 模块 E2E + 共享契约 | pytest + Playwright | `tests/test_pX.py` + `tests/test_contract.py` |
| 打包回归 | 起 EXE + 探测 | 打包后验证 |

---

## 验收分工

| 验收 | 负责人 |
|------|--------|
| `--web` 浏览器模式全功能 | Linux/Wine 负责人 |
| GUI 窗口（WebView2）真机 | Windows 成员 |
| 打包 EXE | Linux/Wine（Wine 跑 Windows Python + PyInstaller）|

> PyInstaller 不跨平台：Linux 打不出 Windows EXE，须 Wine 或 Windows 环境。

---

## 待确认（数据到手后细化）

- 挑战杯主题（决定文案/指标/故事线）
- 数据集清单与字段契约
- 组合查询页交互设计

---

## 2 天时间线

**里程碑排期（按岗位分列）→ 见 `分工说明.md`。**
