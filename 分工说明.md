# 分工说明（5 人岗位划分）

> 与 `协作说明.md`（执行手册）相区别：本文件只讲**谁干什么、依赖关系、关键路径**。
> 所有权表、Git 流程、测试命令等执行细节见 `协作说明.md`。

---

## 岗位职责表

**职责边界：计算归数据岗（A），展示归展示岗（B），页面归 C/D，集成归 E。**
**A 与 B 分开** —— 防止数据加工错误被展示掩盖；展示岗严格对照契约文档与数据流开发。

| # | 岗位 | 核心职责 | 页面 | 测试职责 | 平台 |
|---|------|----------|------|---------|------|
| A | **数据岗** | 数据盘点、pandas ETL、数据契约、数据质量校验；**数据计算层设计**：筛选/聚合/统计/格式化/聚合→图表适配（filter.js、format.js 等，具体 API 由 A 设计）| 首页 `p1` | **数据质量测试**（自查）+ 计算层单测 + `test_p1.py` | Win/WSL |
| B | **展示岗** | `chart-view.js` 统一图表组件 + 看板（只渲染，不碰计算；**组件接口由 B 设计**）| 看板 `p2` | `chart-view.js` 单测 + `test_p2.py` | Win/WSL |
| C | 前端 | 组合查询页（核心）→ 用 A 的计算层 + B 的图表组件 | `p3` | `test_p3.py`（页面 E2E）| Win/WSL |
| D | 前端 | 明细/搜索页 → 用 A 的计算层 | `p4` | `test_p4.py`（页面 E2E）| Win/WSL |
| E | 集成 | 结论页 + 打包 + 整合 + 合并审批 | `p5` | `test_contract.py`（跨模块一致性）+ 打包回归 | Linux(Wine) |

> **数据计算统一归 A 设计**：页面只调用 A 提供的计算层，**禁止在页面里自写筛选/聚合/格式化逻辑**；发现计算层缺功能 → 向 A 提，A 扩展后合入。
> 每模块成员：自己的交付写完，用自家 AI 生成该模块测试，跑绿才算交付。

---

## 数据流分工（上下游关系）

```
A 产数据（raw JSON + 契约 + 计算函数）──► 各页 Data.load 取数 + 用 A 的计算函数 ──► B 的 chart-view 渲染
     │                                                            │
     └── 契约字段 + 计算 API = 所有人的取数/计算依据             └── 图表组件 = 渲染依据
```

### 依赖图
| 成员 | 依赖 | 上游阻塞点 |
|------|------|-----------|
| A（数据岗）| 无上游 | 无（但产出是全场前提）|
| B（展示岗）| A 的契约 | 契约未定则看板数据不可用 |
| C（p3）| A 的契约 + 计算函数 + B 的 chart-view | 契约 + 计算 API + 图表组件 |
| D（p4）| A 的契约 + 计算函数 | 契约 + 计算 API |
| E（集成）| 全部模块 | 任何模块晚交付 |

---

## 关键路径与缓解（务必遵守）

### 1. B 的 chart-view 是全场阻塞点 → 第一优先级冻结接口
- chart-view.js 骨架已就绪且测试绿
- **B 的第一优先级 = 冻结 chart-view 接口**（明确入参/数据形态 `[{label, value}]`、返回方法），再去完善 p2 看板
- 理由：C/D 的图表都依赖它，接口不定则 C/D 图表全卡

### 2. A 是唯一上游 → 分两阶段交付
- **阶段一（优先）：契约骨架** —— 字段名 / 类型 / 可筛选字段（数据暂用样例顶替）
- **阶段二：真实数据** —— pandas ETL 产出正式 JSON
- 契约骨架一出，B/C/D 立即并行开工，**不用等完整数据**

### 3. C 依赖 A 和 B → 先用样例字段开发 UI 骨架
- query-builder 的控件生成逻辑**与具体字段无关**（按类型生成）
- C 可先用样例字段开发查询 UI 骨架，契约一到只改配置

### 4. 测试职责边界（防角色冲突）
- **数据质量测试 + 计算层单测由 A 写**（数据岗自查：行数/空值/字段类型/值域 + 计算函数正确性）
- **C/D 只写自己页面的 E2E**（test_p3.py / test_p4.py），不承担计算层测试
- **test_contract.py 由 E 写**（只管跨模块一致性：文件存在/引用/导航/JSON 合法）
- 分工依据：数据与计算对不对，A 负责自查；公共接口是否一致，E 负责把关

---

## 里程碑（2 天）

| 时间 | A（数据岗）| B（展示岗）| C | D | E（集成）|
|------|-----------|-----------|----|----|---------|
| Day1 上午 | 数据盘点 + 契约骨架 + 计算库 API | 冻结 chart-view 接口 | p3 UI 骨架（样例字段）| p4 UI 骨架 | 搭骨架（已完成）|
| Day1 下午 | pandas ETL + 计算函数 + 数据质量测试 | p2 看板 | p3 功能（接契约/计算 API）| p4 功能 | 并行支持 |
| Day2 上午 | 数据修正 + 计算库补缺 + 收尾 | 图表联调 | p3 图表联动 | 搜索/分页收尾 | 全站联调 |
| Day2 下午 | — | — | — | — | 合并 → 编译 Tailwind → 打包 EXE → 全站回归 |

> 各人自行开发时：`uv run python -m http.server 8000` 开浏览器实操检查。
